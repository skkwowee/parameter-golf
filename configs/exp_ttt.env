# Experiment: Test-Time Training (full-param adaptation on val chunks)
# Status: TESTING — only in top2
# Score first, then adapt on already-scored chunks. Legal per challenge rules.
TTT_ENABLED=1
TTT_LR=0.002
TTT_EPOCHS=3
TTT_CHUNK_TOKENS=32768
TTT_MOMENTUM=0.9
TTT_GRAD_CLIP=1.0
