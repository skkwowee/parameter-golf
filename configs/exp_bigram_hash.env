# Experiment: Bigram hash embeddings
# Status: BASELINE — 2048x128 is now the default (matches both leaders)
# Provides cheap n-gram features alongside transformer
BIGRAM_VOCAB_SIZE=2048
BIGRAM_DIM=128
TRIGRAM=0               # trigram extension (risky, off by default)
