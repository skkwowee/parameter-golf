# Experiment: XSA (Cross-Sequence Attention) on all layers
# Status: ACTIVE — included in top1/top2
# Applied on top of tuned_baseline.env
XSA_LAST_N=11          # XSA on all 11 layers (novel contribution)
